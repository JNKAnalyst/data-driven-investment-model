# Copy to `.env` and fill in values as needed.
# This project does not require any secrets out of the box; the variables below
# are placeholders for users who plug in a paid data source.

# Example: API key for a market-data provider
# MARKET_DATA_API_KEY=
